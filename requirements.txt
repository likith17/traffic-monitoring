# Runtime dependencies. This is what the Docker image installs, and it is
# deliberately free of PyTorch: the container only runs inference, and that
# goes through ONNX Runtime (see routing/detect.py). Torch was 728 MB of the
# 3.27 GB image, measured inside the built container.
#
# To retrain, re-export the ONNX model, or run the ONNX-vs-PyTorch parity
# test, also install requirements-dev.txt.

numpy==2.2.6
pandas==2.3.3
requests==2.32.5
networkx==3.6.1
opencv-python-headless==4.12.0.88
streamlit==1.52.1
folium==0.20.0
streamlit-folium==0.27.2
streamlit-searchbox==0.1.24
osmnx==2.1.0
onnxruntime==1.27.0
